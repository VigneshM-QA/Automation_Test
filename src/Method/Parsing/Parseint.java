package Method.Parsing;

public class Parseint {
    public static void main(String[] args) {
    } {
        // int age = 26;
        String[] args = new String[0];
        int age = Integer.parseInt(args[0]); // chaning in the Edit configuration settings by providing the age value and run
        System.out.println(age > 25 ? "Allowed to In" : "Not allowed to In");
    }
}
